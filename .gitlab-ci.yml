image: node:20-alpine

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - rm -rf public_html
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
